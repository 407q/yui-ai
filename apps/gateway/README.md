# Gateway

Gateway は以下の論理コンポーネントで構成します。

実行前提: Gateway 系コンポーネントはホストマシン上の Node.js プロセスとして動作します。

- `api`（Discord Bot / API）
- `mcp`（tool call 受口）
- `attachments`
- `approvals`
- `state`
- `memory`
- `container-tools`
